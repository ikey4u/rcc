[package]
name = "native-stack-linux"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false
description = "OpenSSL + bundled SQLite used to exercise cargo-rcc on a larger native stack"

[workspace]

[dependencies]
openssl = { version = "0.10", features = ["vendored"] }
rusqlite = { version = "0.32", features = ["bundled"] }
